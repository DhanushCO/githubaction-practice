name: hello world
on: 
  push:
    branches:
    - main
    - feature
  pull_request:
    branches:
    - main
    - feature

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: echo "welcome to github action course"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps: 
      - name: test
        run: echo "yes"

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
     - name: deploy
       run: echo "deploy"
  
    
  
